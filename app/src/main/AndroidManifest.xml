<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.ail.psi">
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<application
    android:name=".app.AppController"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity
        android:name=".Login"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".MainActivity"/>
        <!--android:theme="@style/AppTheme.NoActionBar"/>-->
    <activity android:name=".Register" />
    <activity
        android:name=".monitoring"
        android:theme="@style/AppTheme" />
    <activity
    android:name=".listmonitoring"
    android:theme="@style/AppTheme" />

    <activity
        android:name=".datailHasilmonitoring"
        android:theme="@style/AppTheme" />

    <activity
        android:name=".updateLahan"
        android:theme="@style/AppTheme" />

    <activity
        android:name=".detailmonitoring"
        android:theme="@style/AppTheme" />

    <activity
        android:name=".forgetpassword"
        android:theme="@style/AppTheme" />


    <activity
        android:name=".graphFragment"
        android:theme="@style/AppTheme" />
    <activity
        android:name=".updateuser"
        android:theme="@style/AppTheme" />




</application>

</manifest>